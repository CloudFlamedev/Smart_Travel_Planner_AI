# Dummy placeholder only — never commit real secrets to git.
# In practice: create this via `kubectl create secret` from CI/CD,
# or better, use External Secrets Operator / AWS Secrets Manager sync.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: smart-travel-planner
type: Opaque
stringData:
  GROQ_API_KEY: "REPLACE_ME"
